{
  "name": "book-management-infrastructure",
  "version": "1.0.0",
  "description": "Infrastructure code for Book Management API",
  "main": "lib/infrastructure-stack.js",
  "scripts": {
    "build": "tsc && npm run copy-files",
    "watch": "tsc -w",
    "cdk": "cdk",
    "copy-files": "cp -r secrets dist && cp .sops.yaml dist"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.124.0",
    "constructs": "^10.3.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.11.24",
    "js-yaml": "^4.1.0",
    "typescript": "^5.3.3"
  }
}
